import { Loader } from "../components/loader/Loader"

export function AdminPage() {
    return (
        <div className="bg-white fixed z-50 inset-x-0 inset-y-0 flex flex-col ">
            <h1 className="text-center text-4xl font-bold relative top-[50%]">Admin page is under development.</h1>
            <Loader />
        </div>
    )
}